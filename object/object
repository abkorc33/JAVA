다음 중 생성자에 대한 설명으로 옳지 않은 것을 골라주세요.
	2, 5
1. 모든 생성자의 이름은 클래스의 이름과 동일해야한다.
2. 생성자는 객체를 생성하기 위한 것이다.
3. 클래스에는 생성자가 반드시 하나 이상 있어야 한다.
4. 생성자가 없는 클래스는 컴파일러가 기본 생성자를 추가해 준다.
5. 생성자는 오버로딩 할 수 없다.

다음 중 this 설명중 틀린것 찾기
	2
1. 객체 자신을 가리키는 참조변수이다.
2. 클래스 내에서라면 어디서든 사용할 수 있다	//인스턴스 메서드에서만 사용이 가능하다.
3. 지역변수와 인스턴스 변수를 구별할 때 사용한다.
4. 클래스(static) 매서드 내에서는 사용할 수 없다.

다음 중 오버로딩이 성립하기 위한 조건이 아닌것을 모두 고르기
	3, 4
1. 메서드의 이름이 같아야한다
2. 매개변수의 개수나 타입이 달라야 한다
3. 리턴타입이 달라야한다
4. 매개변수의 이름이 달라야 한다

다음 중 아래의 plus메서드를 올바르게 오버로딩한 것을 고르시오.
	2, 3, 4
long plus(int a, int b) { return a+b; }

1. long plus(int x, int y) { return x + y; }
2. long plus(long a, long b) {return a + b; }
3. int plus(byte a, byte b) { return a+ b; }
4. int plus(long a, int b) { return (int)(a+b); }

다음 중 초기화에 대한 설명으로 옳지 않은 것은
	3, 5
1. 멤버변수는 자동 초기화되므로 초기화하지 않고도 값을 참조할 수 있다.
//인스턴스변수, 클래스변수, 배열은 자동으로 초기화 됨
2. 지역변수는 반드시 초기화해야한다.
3. 초기화 블럭보다 생성자가 먼저 실행된다.
4. 명시적 초기화를 제일 먼저 고려해야한다.
5. 클래스변수보다 인스턴스변수가 먼저 초기화된다.

다음 중 지역변수에 대한 설명으로 옳지 않은 것을 모두 고르시오.
	1, 5
1. 자동 초기화 되므로 별도의 초기화가 필요 없다.
2. 지역변수가 선언된 메서드가 종료되면 지역변수도 함께 소멸된다.
3. 메서드의 매개변수로 선언된 변수도 지역변수이다.
4. 클래스변수나 인스턴스변수보다 메모리의 부담이 적다.
5. 힙(heap)영역에 생성이 되며 가비지 컬렉터에 의해서 소멸이 된다.
//힙 영역은 인스턴스 변수가 들어가는 영역, 지역변수는 stack영역